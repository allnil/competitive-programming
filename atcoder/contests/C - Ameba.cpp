#include <stdio.h>
#include <vector>

using namespace std;

vector<int> g(200050, -1);

int main(){
  int n; scanf("%d", &n);
  int root, a; scanf("%d", &a);

  int i = 0;
  while(n--){

  }

  return 0;
}